export const showCrimes = (crimes) => ({
  type: 'SHOW_CRIMES',
  crimes
})

export const showTeams = (teams) => ({
  type: 'SHOW_TEAMS',
  teams
})

export const showPlayers = (players) => ({
  type: 'SHOW_PLAYERS',
  players
})